<%= form_for([@itemable, @item]) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :amount %>
    <%= f.text_field :amount %>

    <%= hidden_field_tag @itemable_id, @itemable.id %>

    <%= f.submit %>

<% end %>
