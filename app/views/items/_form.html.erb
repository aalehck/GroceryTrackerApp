<%= form_for ([@grocery_list, @grocery_list.items.build]) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :list_amount %>
    <%= f.text_field :list_amount %>

    <%= f.submit %>

<% end %>
