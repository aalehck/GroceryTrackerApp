<td><%= link_to item.name, polymorphic_path([@itemable, item], @itemable_id => @itemable.id, :id => item.id, method: :show)%></td>
<td><%= item.amount.round(2) %></td>
<td><%= item.unit %></td>
<td><%= link_to image_tag('help_x_btn.png', size: "14x14"), polymorphic_path([@itemable, item], @itemable_id => @itemable.id, :id => item.id),
    method: :delete,
    data: { confirm: 'Are you sure?' } %></td>
   
